import { defineStore } from 'pinia'

export const {{ camelCase name }}Store = defineStore('{{ camelCase name }}', () => {

  return {}
}{{#if persist}},{
  persist: {
    key: '{{ camelCase name }}-db',
    storage: sessionStorage
  }
}{{/if}})
